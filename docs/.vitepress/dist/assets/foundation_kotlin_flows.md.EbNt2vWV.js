import{_ as t,o as a,c as i,ag as s}from"./chunks/framework.BKDXG6j6.js";const u=JSON.parse('{"title":"Kotlin Flows","description":"","frontmatter":{},"headers":[],"relativePath":"foundation/kotlin/flows.md","filePath":"foundation/kotlin/flows.md","lastUpdated":1774424426000}'),o={name:"foundation/kotlin/flows.md"};function l(n,e,r,c,d,h){return a(),i("div",null,[...e[0]||(e[0]=[s(`<h1 id="kotlin-flows" tabindex="-1">Kotlin Flows <a class="header-anchor" href="#kotlin-flows" aria-label="Permalink to &quot;Kotlin Flows&quot;">​</a></h1><p>Flow is an asynchronous data stream that sequentially emits values and completes normally or with an exception. It is built on top of Coroutines and is meant to be a reactive programming alternative to RxJava.</p><h2 id="core-concepts" tabindex="-1">Core Concepts <a class="header-anchor" href="#core-concepts" aria-label="Permalink to &quot;Core Concepts&quot;">​</a></h2><h3 id="_1-producers-intermediaries-consumers" tabindex="-1">1. Producers, Intermediaries, Consumers <a class="header-anchor" href="#_1-producers-intermediaries-consumers" aria-label="Permalink to &quot;1. Producers, Intermediaries, Consumers&quot;">​</a></h3><ul><li><strong>Producer</strong>: Emits data into the stream (<code>flow { emit(value) }</code>).</li><li><strong>Intermediaries</strong>: Modify the stream (e.g., <code>map</code>, <code>filter</code>). These are cold operators; they don&#39;t execute until the flow is collected.</li><li><strong>Consumer</strong>: Receives the values (e.g., <code>collect</code>).</li></ul><h3 id="_2-cold-vs-hot-flows" tabindex="-1">2. Cold vs. Hot Flows <a class="header-anchor" href="#_2-cold-vs-hot-flows" aria-label="Permalink to &quot;2. Cold vs. Hot Flows&quot;">​</a></h3><ul><li><strong>Cold Flow (<code>Flow&lt;T&gt;</code>)</strong>: Code inside a flow builder does not run until the flow is collected. Every call to <code>collect</code> starts the execution from the beginning.</li><li><strong>Hot Flow (<code>StateFlow</code>, <code>SharedFlow</code>)</strong>: Active regardless of collectors. Existing data or events can be emitted to multiple collectors simultaneously.</li></ul><h3 id="_3-stateflow" tabindex="-1">3. StateFlow <a class="header-anchor" href="#_3-stateflow" aria-label="Permalink to &quot;3. StateFlow&quot;">​</a></h3><p>A state-holder observable flow that emits the current and new state updates to its collectors. It requires an initial state.</p><ul><li>Perfect for UI State in ViewModels (replaces <code>LiveData</code>).</li><li>Conflates emissions (if updating state fast, slow collectors only get the latest state).</li></ul><h3 id="_4-sharedflow" tabindex="-1">4. SharedFlow <a class="header-anchor" href="#_4-sharedflow" aria-label="Permalink to &quot;4. SharedFlow&quot;">​</a></h3><p>A highly configurable hot flow that emits events to all subscribers.</p><ul><li>Doesn&#39;t require an initial value.</li><li>Useful for one-time events (e.g., showing a Snackbar or observing navigation commands).</li></ul><h2 id="best-practices-in-android" tabindex="-1">Best Practices in Android <a class="header-anchor" href="#best-practices-in-android" aria-label="Permalink to &quot;Best Practices in Android&quot;">​</a></h2><h3 id="collecting-in-ui" tabindex="-1">Collecting in UI <a class="header-anchor" href="#collecting-in-ui" aria-label="Permalink to &quot;Collecting in UI&quot;">​</a></h3><p>Never collect flows directly in standard UI using just <code>launch</code>, as it runs even when the app is in the background, wasting resources and potentially crashing.</p><ul><li><strong>Compose</strong>: Use <code>collectAsStateWithLifecycle()</code>.</li><li><strong>Views</strong>: Use <code>viewLifecycleOwner.lifecycleScope.launch</code> combined with <code>repeatOnLifecycle(Lifecycle.State.STARTED)</code>.</li></ul><div class="language-kotlin vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">kotlin</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// In a Fragment/Activity</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">lifecycleScope.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">launch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    repeatOnLifecycle</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(Lifecycle.State.STARTED) {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        viewModel.uiState.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">collect</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { state </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">            // Update UI</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="interview-questions" tabindex="-1">Interview Questions <a class="header-anchor" href="#interview-questions" aria-label="Permalink to &quot;Interview Questions&quot;">​</a></h2><p><strong>Q: What is the difference between <code>StateFlow</code> and <code>SharedFlow</code>?</strong><em>Answer:</em> <code>StateFlow</code> is a specialized <code>SharedFlow</code> that requires an initial value, holds exactly one value (the current state), and conflates emissions (subscribers only get the latest state). <code>SharedFlow</code> is highly configurable, doesn&#39;t require an initial value, and is typically used to handle one-time events (e.g., navigation paths).</p><p><strong>Q: Why use Flow instead of LiveData?</strong><em>Answer:</em></p><ul><li>Flow is part of the Kotlin language, making it usable in domain layers void of Android dependencies (Clean Architecture).</li><li>LiveData is strictly tied to the Main thread.</li><li>Flow offers a rich set of operators (<code>map</code>, <code>filter</code>, <code>combine</code>, <code>flatmapLatest</code>).</li><li><code>StateFlow</code> and <code>SharedFlow</code> offer all the features of LiveData along with better thread configurations and control.</li></ul>`,22)])])}const f=t(o,[["render",l]]);export{u as __pageData,f as default};
